This folder contains the basic server implementation bits. 

This should probably not be touched, unless you're implementing some kind of 
cross-cutting stuff, like e.g. logging of requests, custom authentication 
(possibly based on attributes on handlers?), etc.